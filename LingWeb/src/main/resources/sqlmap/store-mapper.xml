<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="store">

<select id="store_select" resultType="store.StoreVO">

select i.*,(item_price*sales) as sales_amount from store_item i

</select>

<select id="store_select_totalsales" resultType="Integer">
select sum(item_price*sales) as total_sales from store_item
</select>


</mapper>